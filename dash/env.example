# Copy to .env in this directory and fill in. chmod 600. Never commit.
STUDY_SMS_NUMBER=+1 (507) 431-7807
ORG_NAME=Child Mind Institute
CONTACT_EMAIL=olivia.fitzpatrick@childmind.org
PHONE_HASH_SALT=              # openssl rand -hex 16 -- set once, never rotate
ADMIN_TOKEN=                  # openssl rand -hex 24
PROLIFIC_CC_COMPLETE=
PROLIFIC_CC_ATTENTION=
PROLIFIC_CC_NO_CONSENT=

# Retell dashboard API key, and the chat agent it authenticates against.
# This one key covers everything: the opt-in confirmation message and any
# other call to Retell. The agent id is not a secret -- it is useless without
# the key -- so it is filled in here rather than left blank.
#
# Do not put an inline comment after the value on the same line. Compose
# strips them correctly, but every other way of reading the file does not.
RETELL_API_KEY=
RETELL_AGENT_ID=agent_ffd9f3ee004a2f91bd92bbc378

# Only needed to override the defaults: the send endpoint already points at
# Retell, and the token already falls back to RETELL_API_KEY. Until a key is
# set, /api/opt-in records each consent and reports the confirmation as
# "unconfigured" rather than claiming it was sent.
#
# Two things to know about the send: the first message is authored by the
# outbound agent bound to the study number, so that agent's begin message
# must be the registered confirmation text; and carriers filter outbound A2P
# messages until the campaign is approved, so a "sent" result before then
# means accepted by Retell, not delivered.
# SMS_SEND_URL=
# SMS_SEND_TOKEN=

# Where the opt-in page is published. OPTIN_PAGE_ORIGIN is the only origin
# allowed to POST to /api/opt-in from a browser; if the page moves, change it
# here or the submission is blocked by CORS.
OPTIN_PAGE_URL=https://matter.childmind.org/studies/dash/opt-in/
OPTIN_PAGE_ORIGIN=https://matter.childmind.org
